# svg_path_fsharp Markdown Assets

This orphan branch stores SVG figures used by Markdown documents in the
`svg_path_fsharp` repository.

The package README can reference these files through stable raw GitHub URLs,
for example:

```text
https://raw.githubusercontent.com/vistuleB/svg_path_fsharp/markdown-assets/figures/name.svg
```
