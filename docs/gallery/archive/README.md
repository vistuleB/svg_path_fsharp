# Historical Gallery snapshots

`second-offset-arrangement-2026-08-22.svg` is the old precomputed diagnostic,
preserved for comparison. It is never read by the current generator. The
published second-offset figure now captures the actual F# production pipeline
in a diagnostic-only build.
