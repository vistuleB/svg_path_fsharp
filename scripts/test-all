#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Running the full test suite (fast then slow profiles)."
"$root/scripts/test-fast"
"$root/scripts/test-slow"
