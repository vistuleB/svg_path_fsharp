#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "Running slow test profile (convex-hull stress suite only)."
dotnet test "$root/tests/SvgPath.Tests/SvgPath.Tests.fsproj" --filter "Category=Slow"
